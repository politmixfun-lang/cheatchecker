# Mine Checker работает на чистой стандартной библиотеке Python.
# Эти пакеты нужны только для красивого интерфейса и сборки в один файл.

customtkinter>=5.2      # современные скруглённые контролы (без него — обычный tkinter)
pyinstaller>=6.0        # сборка MineChecker.exe / MineChecker.app
